# Omega PC Remote — Environment Variables
# Copy to .env.local in the project root

# WebSocket Relay Server URL (Render)
NEXT_PUBLIC_RELAY_URL=wss://omega-relay.onrender.com

# Relay Token (get from Render logs after deploy)
NEXT_PUBLIC_RELAY_TOKEN=your-relay-token-here
